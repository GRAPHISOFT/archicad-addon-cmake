name: Release Add-On Pipeline

# on: workflow_dispatch
on: 
  push:
    branches: [ "pipeline-integration-test" ]

defaults:
  run:
    shell: bash

jobs:
  build:
    runs-on: ${{matrix.os-type}}
    outputs:
      releaseVersion: ${{ steps.setVersion.outputs.version }}

    strategy:
      matrix:
        os-type: [ windows-2019, macos-latest ]
      fail-fast: false

    steps:
      - uses: actions/checkout@v3

      - name: Checkout the submodule
        uses: actions/checkout@v3
        with:
          submodules: recursive

      - name: setup python
        uses: actions/setup-python@v2
        with:
          python-version: 3.8
      
      - name: Run build script
        run: |
          python Tools/buildAddOn.py --configFile .\config.json --acVersion 27 --release --package

      - name: Store artifact
        uses: actions/upload-artifact@v3
        with:
          name: ${{ github.event.repository.name }}-${{ matrix.os-type }}-Release-localized
          path: "${{ github.workspace }}/Build/Package/*.zip"
  
  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v3
        with:
          path: "${{ github.workspace }}/artifacts"

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: "test"
          files: "${{ github.workspace }}/artifacts/*/*.zip"
            
            
      
